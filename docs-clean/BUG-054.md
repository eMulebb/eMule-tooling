---
id: BUG-054
title: ESC in shared-file delete confirmation could still delete files
status: Done
priority: Major
category: bug
labels: [shared-files, delete, confirmation, data-loss, emuleai-port]
milestone: ~
created: 2026-04-25
source: current `main` commit `76686b7`; `analysis\emuleai` v1.4 notes
---

## Summary

The shared-file delete confirmation treated only `IDNO` as cancellation, so
`IDCANCEL` from ESC or the close button could fall through.

## Mainline Outcome

Landed on `main` in commit `76686b7` (`BUG-054 require shared file delete
confirmation`).

The delete path now requires `IDYES` explicitly.
