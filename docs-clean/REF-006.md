---
id: REF-006
title: GetCategory should be const in DownloadListCtrl — acknowledged but unfixed
status: Open
priority: Trivial
category: refactor
labels: [const-correctness, api-design]
milestone: ~
created: 2026-04-08
source: AUDIT-DEADCODE.md (section 4.3)
---

## Summary

`srchybrid/DownloadListCtrl.cpp:1756` contains:

```cpp
// 'GetCategory' SHOULD be a 'const' function
```

The method is called in a const context but was not declared `const`.
The comment acknowledges the issue but it was never fixed. This is a
const-correctness API debt item.

## Fix

1. Locate `GetCategory()` declaration in `DownloadListCtrl.h`.
2. Add `const` qualifier.
3. Verify all call sites compile cleanly.
4. Remove the comment.

## Files

- `srchybrid/DownloadListCtrl.cpp:1756`
- `srchybrid/DownloadListCtrl.h` — declaration to fix
