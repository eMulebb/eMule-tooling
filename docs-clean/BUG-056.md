---
id: BUG-056
title: Download Clients list could dereference stale rows during display callbacks
status: Done
priority: Major
category: bug
labels: [downloads, clients, stale-pointer, crash, emuleai-port]
milestone: ~
created: 2026-04-25
source: current `main` commit `738302f`; `analysis\emuleai` v1.4 notes
---

## Summary

The Download Clients list still had a display callback path that trusted cached
row payloads.

## Mainline Outcome

Landed on `main` in commit `738302f` (`BUG-056 guard download clients stale
rows`).

The display path now uses current item data and live ownership checks before
formatting stale rows.
