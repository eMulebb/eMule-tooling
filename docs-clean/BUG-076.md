---
id: BUG-076
title: WebServer malformed request hardening for REST and legacy HTML
status: Open
priority: Major
category: bug
labels: [webserver, rest, malformed-input, robustness]
milestone: broadband-release
created: 2026-05-02
source: broadband release live E2E and REST completeness planning
---

## Summary

Harden malformed request handling at the WebServer boundary while preserving
valid legacy HTML and REST behavior.

## Release 1.0 Classification

**Release Gate.** This must land before the first public release because the
REST API and legacy HTML server share the same embedded WebServer boundary.
Malformed REST requests must fail as REST without falling into legacy
login/session behavior.

## Acceptance Criteria

- [ ] invalid JSON, non-object JSON, truncated body, unsupported method, and
      unsupported route are tested
- [ ] content-type mismatches and encoded path oddities do not escape the REST
      routing model
- [ ] oversized or suspicious request bodies fail cleanly without large
      allocation spikes
- [ ] REST routes never fall back to HTML login/session behavior
- [ ] legacy HTML routes still render after REST hardening

## Relationship To Other Items

- gates `CI-015`
- complements `BUG-069`, `BUG-073`, and `BUG-075`
