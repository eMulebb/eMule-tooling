---
id: BUG-063
title: ESC in shared-directory delete confirmation could still delete directories
status: Done
priority: Major
category: bug
labels: [shared-directories, delete, confirmation, data-loss]
milestone: ~
created: 2026-04-25
source: current `main` commit `7cf1a7b`
---

## Summary

The shared-directory delete confirmation needed the same explicit `IDYES`
requirement as shared-file deletion.

## Mainline Outcome

Landed on `main` in commit `7cf1a7b` (`BUG-063 require shared-dir delete
confirmation`).

The delete path no longer treats ESC or dialog close as confirmation.
