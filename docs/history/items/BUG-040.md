---
id: BUG-040
title: Downloading Clients list could dereference stale client rows
status: Done
priority: Major
category: bug
labels: [transfers, downloading-clients, stale-pointer, crash]
milestone: ~
created: 2026-04-25
source: current `main` commit `1eea13a`
---

## Summary

The Downloading Clients list trusted row payload pointers after the backing
client/source state could have changed.

## Mainline Outcome

Landed on `main` in commit `1eea13a` (`BUG-040 guard downloading-clients stale
rows`).

The list now validates row payloads against live ownership before formatting or
drawing stale clients.
