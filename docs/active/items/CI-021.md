---
id: CI-021
title: WebSocket and legacy socket leak-churn gate
status: Open
priority: Major
category: ci
labels: [testing, websocket, casyncsocket, leak, stress, r1-gate]
milestone: broadband-release
created: 2026-05-08
source: R1 SDET stability review for legacy socket leak and reset coverage
---

## Summary

Add an R1 release gate that repeatedly churns WebSocket, REST, and legacy socket
close paths while measuring handles, threads, queued socket state, and process
memory.

This is the release proof for known legacy Win32/MFC socket hazards: accepted
client lifetime, CAsyncSocket-style helper state, async close/reset ordering,
queued send cleanup, and thread-object ownership.

## Risk Being Covered

- accepted-client or listener thread objects leaking after socket churn
- helper-window or async-socket state retaining stale socket pointers
- reset/close races leaving queued send buffers attached to dead sockets
- self-deleting socket objects running callbacks after owner teardown
- process handle/thread/private-byte growth hidden by otherwise passing request
  counts

## Execution Plan

1. Add a leak-churn harness mode that records baseline process handles, threads,
   GDI objects, USER objects, private bytes, and accepted WebSocket thread
   counts.
2. Run repeated HTTP and HTTPS connect/reset cycles, including idle accepted
   sockets, partial headers, partial declared bodies, slow responses, and
   queued-response disconnects.
3. Run legacy listen-socket churn for short-lived accepted peer-like sockets
   where the harness can do so without joining the public network.
4. Run stop/start cycles after churn to prove WebSocket global state is reusable
   only after accepted clients drain.
5. Compare post-drain resource counts against baseline and documented tolerance.
6. Emit a release artifact containing counts before churn, peak counts, counts
   after drain, socket outcome totals, and any stuck thread/handle identifiers.

## Acceptance Criteria

- [ ] 1k+ HTTP connect/reset cycles complete without unbounded handle, thread,
      or memory growth
- [ ] 1k+ HTTPS connect/reset cycles complete without unbounded handle, thread,
      or memory growth
- [ ] accepted-client threads drain before WebSocket termination state is closed
- [ ] queued send buffers are released when clients reset during response send
- [ ] stop/start after churn succeeds only from a fully drained state
- [ ] leak-churn reports include baseline, peak, and post-drain process
      resource counts

## Relationship To Other Items

- validates [BUG-079](BUG-079.md), [BUG-080](BUG-080.md),
  [BUG-088](BUG-088.md), [BUG-095](BUG-095.md), and [BUG-099](BUG-099.md)
- complements [CI-019](CI-019.md) with resource-accounting proof
- tracked by the
  [Release 1.0 SDET stress execution plan](../plans/RELEASE-1.0-SDET-STRESS-EXECUTION-PLAN.md)
