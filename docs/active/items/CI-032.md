---
id: CI-032
title: Release 1.0.1 post-tag focused coverage gaps
status: Open
priority: Major
category: ci
labels: [r1.0.1, release-gate, regression, coverage, post-tag-hardening]
milestone: R-1.0.1
created: 2026-05-09
source: R-1.0.1 post-1.0 hardening replay
---

## Summary

Add or identify focused regression probes for post-1.0 fixes that currently
have only indirect replay evidence. This item is release-blocking because the
fixes are patch-release hardening commits and should not rely only on broad
build/native replay.

## Required Coverage

- BUG-102: prove DirectDownload cancellation cannot register a new WinInet
  handle after owner cancellation has already consumed the cancellation state.
- BUG-105: prove the WebSocket listener continues draining pending accepted
  sockets after rejecting a disallowed remote-access IP.
- BUG-108: prove autocomplete enumeration returns `E_OUTOFMEMORY` without
  copying through a null `CoTaskMemAlloc` result.
- BUG-109: prove meter-icon creation releases GDI brushes, pens, bitmaps, and
  scratch DCs on success and early-return failures.
- BUG-110: prove meter-icon bitmaps are deselected from scratch DCs before
  `CreateIconIndirect`.

## Acceptance Criteria

- [ ] Each required coverage target has a focused native test, Python smoke,
      live smoke, or documented existing evidence that directly exercises the
      patched behavior.
- [ ] New tests run through supported `workspace.ps1` commands.
- [ ] The Release x64 replay remains green after the new probes.
- [ ] Any confirmed product defect found while adding coverage becomes a
      `BUG-111+` item.
- [ ] [CI-023](CI-023.md) and
      [R-1.0.1 post-tag replay](../reviews/REVIEW-2026-05-09-release-1.0.1-post-tag-hardening-replay.md)
      are updated with final evidence.

## Validation

- `pwsh -File EMULE_WORKSPACE_ROOT\repos\eMule-build\workspace.ps1 build-tests -Config Release -Platform x64`
- `pwsh -File EMULE_WORKSPACE_ROOT\repos\eMule-build\workspace.ps1 test -Config Release -Platform x64`
- Additional focused commands added by the implementation slice.

## Relationship To Other Items

- created by [CI-023](CI-023.md)
- feeds [CI-025](CI-025.md), [CI-027](CI-027.md), [CI-029](CI-029.md), and
  [CI-030](CI-030.md)
